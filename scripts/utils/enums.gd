class_name Enums extends Node

enum PlayerMoves {
	JUMP,
	RIGHT,
	LEFT
}

enum OrbEvent {
	GENERIC,
	ADD_LIFE
}

enum SoundType {
	MUSIC,
	SFX
}

enum Sounds {
	ORB_COLLECTED,
	GAME_OVER,
	LOFI_BG_MUSIC
}
