class_name Enums extends Node

enum PlayerMoves {
	JUMP,
	RIGHT,
	LEFT
}

enum OrbType {
	RED,
	BLUE
}

enum SoundType {
	MUSIC,
	SFX
}

enum Sounds {
	ORB_COLLECTED,
	GAME_OVER,
	LOFI_BG_MUSIC
}

enum ButtonType {
	PLAY,
	SETTINGS,
	PAUSE,
	BACK,
	EXIT,
	TUTORIAL,
	AUDIO,
	VIDEO,
	CONTROLS
}
