class_name Enums extends Node

enum PlayerMoves {
	JUMP,
	RIGHT,
	LEFT
}

enum OrbType {
	RED,
	BLUE
}

enum SoundType {
	MUSIC,
	SFX
}

enum Sounds {
	ORB_COLLECTED,
	GAME_OVER,
	LOFI_BG_MUSIC
}

enum MainButtonType {
	PLAY,
	SETTINGS,
	BACK,
	EXIT,
}

enum MainScene {
	WORLD_BUILDER,
	MAIN_MENU,
	SETTINGS_MENU,
	TUTORIAL
}
